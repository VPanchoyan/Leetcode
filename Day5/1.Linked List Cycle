class Solution {
public:
    bool hasCycle(ListNode *head) {
        ListNode* p = head;
        for(int i = 0; i < 10001; ++i){
            if(p == nullptr) return false;
            p = p->next;
            }
        return true;
    }
};
